package br.com.everaldoboscatto.screenmatch.service.buscas;

import br.com.everaldoboscatto.screenmatch.model.DadosEpisodio;
import br.com.everaldoboscatto.screenmatch.model.DadosSerie;
import br.com.everaldoboscatto.screenmatch.model.DadosTemporada;
import br.com.everaldoboscatto.screenmatch.service.ConsumoAPI;
import br.com.everaldoboscatto.screenmatch.service.ConverterDados;

import java.util.ArrayList;
import java.util.List;

public class StartApi {
    public void excutarBuscas () {
/*
        // Obter o dados da série mias episódio e atribuir a variável json
        String buscarEpisodio = "gilmore+girls&Season=1&episode=2";
        json = consumoAPI.obterDados("https://www.omdbapi.com/?t=" + buscarEpisodio + "&apikey=" + chave);

        // Transformar Json em dados da classe DadosEpisodio
        DadosEpisodio dadosEpisodio = conversor.obterDados(json, DadosEpisodio.class);

        // Transformar Json em dados da classe DadosEpisodio
        System.out.println(dadosEpisodio);

        List<DadosTemporada> listaTemporadas = new ArrayList<>();


        for (int i = 1; i <= dados.totalTemporadas(); i++) {
            json = consumoAPI.obterDados("https://www.omdbapi.com/?t=" + buscarTemporada + i + "&apikey=" + chave);

            DadosTemporada dadosTemporada = conversor.obterDados(json, DadosTemporada.class);

            listaTemporadas.add(dadosTemporada);
        }
        listaTemporadas.forEach(System.out::println);
*

 */
    }
}
